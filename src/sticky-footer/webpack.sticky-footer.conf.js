var baseConfig = require('../common/base.config.js');

module.exports = baseConfig({
	htmlTitle : 'sticky-footer',
	htmlFileURL : 'html/sticky-footer.html',
	appDir : 'js/sticky-footer/',
	uglify: true,
	hash:''
})
